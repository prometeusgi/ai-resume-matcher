spring:
  application:
    name: matcher-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/matcher_db
  mvc:
    throw-exception-if-no-handler-found: true

ai:
  service:
    base-url: http://localhost:8000
    connect-timeout: 2s
    read-timeout: 5s
